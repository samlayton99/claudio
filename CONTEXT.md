# CONTEXT.md

Read this plus `specs/00`–`07` and you know everything that matters. `specs/` is the law; this file is the reasoning, history, and taste behind it. Written for a future agent with zero conversation history.

## What claudio is

A self-describing relational scaffold of one person's life — the **life-harness for his agents**. It ingests every data stream (windows), distills raw into atoms and a biography wiki, serves context as the API (`get_context` packets), aligns everything against a user-authored purpose contract, and maintains itself. It is the context layer, never the harness: external agents (Claude Code, Agent SDK, Hermes-class assistants) are consumers to feed, not competitors to rebuild. The bet: agents, models, and harnesses churn every few months; a clean data model of a life, with its contracts, lasts decades. Own the data, rent the agents.

The apex idea — and the part of this project that is genuinely novel — is **life purpose as a typecheck against reality**: a user-maintained contract (goals, values/beliefs, attributes, a priorities document) that the whole recorded life is continuously checked against. Everything else is downstream plumbing the ecosystem will increasingly commoditize; the purpose contract and the owned scaffold are the durable core. Build order front-loads them.

## Who Sam is, and how to work with him

Stanford ICME master's student, ML researcher in the Hazy Lab (LLMs, reasoning, RL, efficiency). Devout member of the Church of Jesus Christ of Latter-day Saints — faith and family are the top of the hierarchy, full stop; he serves as ward executive secretary (church admin is a real workload). Married to Jamie. Deep in PROD, a founder community: talent-spotting, intros, relationship volume. Has ADHD: overwhelmed by volume (100+ unread is normal), hates maintenance, needs low-friction/high-signal everything. Wants to be "the highest-agency person in the world" — AI to expand surface area while he still understands the machine.

Working with him: be direct and substantive; challenge flawed reasoning — he explicitly punishes sycophancy and rewards honest pushback (several of his best design contributions came after being told he was wrong, and several of his corrections reversed my positions — both directions are expected). Think from first principles; use precise technical language. Never use emojis. Docs extremely concise — he scans. Commit plainly (no Claude attribution, default git author). He reviews specs word-for-word; for big passes he wants a diff-log format: *his words → what changed → where*. His self-declared failure mode: building theoretical systems instead of iterating in person ("assume half of this will be discarded") — when in doubt, cut; he wants over-cutting proposed and will decline individually. Pattern for decisions: choose a sensible default, queue it for review, keep moving; stop only for genuinely user-owned calls.

## How we got here (compressed history)

Two dead predecessors: a Next.js+Supabase "First Principles Dashboard" (worked, then ossified — hand-rolled LLM harness was the mistake; its best ideas survive: event log, draft-don't-send, one-text capture, approval queue) and a planned "Hermes" VPS orchestrator (never built; would have ossified harder). The brainstorm (now `docs/archive/`) produced the principles; then specs went v1 → v4 through: three adversarial reviews (architecture, security, 25-example eval-walk), a 26-finding consistency pass, two deep research passes (`docs/research-traversal.md`, `docs/research-wiki.md` — both validated the core design and sharpened it), four commissioned audits (UX rings → `docs/ux-rings.md`; dependency sweep; malicious-superintelligence red-team; over-engineering pass), Sam's full line-by-line review, and his v4 directives. The honesty audit (`docs/honesty-audit.md`) records the build-or-not verdict: build, under standing gates.

## The reasoning behind the load-bearing decisions

- **The decay test** governs everything: a brand-new agent, given only this repo and the database, must orient and operate correctly in one session. Self-description (COMMENT ON with examples, generated SCHEMA.md with sample rows, this file) is how the system survives tool churn.
- **L1 as syscalls**: agents hold zero direct table writes; SECURITY DEFINER functions validate, clamp, audit. Raw SQL as an agent write surface fails in practice (21% solve rates, injection incidents — research). Reads are SELECT over invoker views under RLS.
- **Atoms** are units of life experience — compact index cards (summary ≤750, detail, verbatim quotes, refs), never documents. Tier ladder: raw (stays at source) → atoms → rollups. Depth scales by destination, not atom size. Record everything non-spam (recording ≠ extracting; a meme-filled family chat day is one honest line that powers the relationship record); extract conservatively. Bias toward thoughtful larger chunks.
- **Importance is structural, never volumetric, and headlines are computed, not chosen.** Sam's deepest recurring concern is LLM taste interrupting human taste ("the LLM thinks X is the most important thing today, so everything is about X"). The defenses: user-set role weights order sections; due-dates order obligations; `notable` is a boolean CHECK-paired with a typed `notable_reason` (vocab-validated by trigger — Sam's P12 ask: binary, deterministic, typechecked; the model selects from a list it didn't write), judged at the daily pass with longitudinal context; "find all instances" is a typed-links COUNT query, never model recall; trend claims must survive an aggregate (candidates-from-summaries, verdicts-from-atoms — the anti-cherry-picking protocol). The model owns wording; never ordering, inclusion, or facts.
- **P8, the fundamental law of LLMs** (Sam's own formulation): quality decays exponentially with nested summaries. Mechanical consequences: compaction depth ≤ 2; every rollup re-derives from atoms (prior summaries are *context, never source* — only trajectory claims may cite them); absolute dates; verbatim quotes for load-bearing facts; re-ground from tier-0 refs before any irreversible action; delta edits, never full-page regeneration.
- **Two-lane scoring** (Sam caught the bug): obligations score by importance × urgency (recency of creation is irrelevant to something due now); background context scores by importance × recency. Cobb-Douglas with floors (his utility intuition; floors prevent a product from zeroing old-but-critical items).
- **Taste is data**: role weights, directives, the purpose contract — all user-set, reaching agents as data. The mirror is the one agent licensed to model taste, and it owns exactly one judgment: how the user is living up to his purpose. It hands taste to no other agent.
- **Security in one breath**: deterministic stops at every boundary; probabilistic layers operate only inside them. OS users are trust tiers; clearance keys on `session_user`; text never escalates privilege; taste writes additionally require the **intent binding** (verbatim-in-raw or edge read-back-confirm — a channel proof is not an intent proof; this closed the deepest red-team finding); capability is issued, never declared; one sender (the edge), destinations hardwired; approvals bind to a server-rendered what-will-execute view, never an agent summary; `suspected_injection` content renders inert everywhere. Assume workers get prompt-injected — intake is adversary-writable by construction.
- **Reliability is absolute for reminders** (P6): LLMs never in a reminder's critical path; degraded-mode delivery (if the model step fails, the deterministic skeleton still sends); watchdog + external dead-man (the alarm must not depend on the alarm channel); critical components never auto-disable; leases on every claim; the system must survive three weeks of total neglect and recover with one rollup, no cleanup project (neglect mode).
- **The wiki is the biography**, not a DB mirror — a wikipedia of the noteworthy, eleven fixed chapters, page bar two-sided (useful to Sam-in-6-months for journaling joy, OR to his agents for a future query). The owner's reading appetite is the scarce resource: pages need a chapter + a read-moment at creation; page count grows sublinearly with atoms. Fabricated citations are the #1 documented AI-wiki defect → lint checks citations exist, the verifier step (fresh context, never the author) checks they support claims; human corrections are immutable spans.
- **Gardener minimax (P9)**: every gardener run is evidence of an upstream design gap — the design denies gardeners work (constraints, idempotence, templated low-signal atoms); the system ensures they run anyway. A shrinking gardener workload is health.
- **Nothing scattered (P10)**: one parameters registry (core/outer rings); one folder per agent (`core/agents/<id>/` with prompt.md + context.md declaring its deterministic context pulls). Tweaking is editing a folder.
- **Staged hardening**: two OS users at P1, full tiers + pf + Tailscale at P3 — security ceremony arrives with its load, because Ring-0 setup burden is the most likely project-killer (the UX rings audit's "sins list" thinking).

## Pitfalls future agents must not reintroduce

1. Building harness machinery (loops, routers, schedulers, message gateways) — the failure that killed both predecessors. The one permitted resident besides the panel is the edge; the orchestrator may run resident only as a configured occupant of its slot.
2. LLM taste leaking into ordering, inclusion, importance, or headlines. The scaffold computes; the model phrases.
3. Summary-of-summary chains, relative dates, paraphrased commitments. P8 is law.
4. Anecdote-driven assessments. Aggregate first, cite both the number and the atoms, label impressions as impressions.
5. Speculative structure: new tables, vocab kinds, parameters, or pages built for imagined volume. Grow by promotion on observed need; shrink by demotion. When the over-engineering instinct fires, it's probably right.
6. Treating channel proof as intent proof anywhere new. Any new write path that could encode taste must carry the intent binding.
7. Mirroring *live external state* (gcal futures, mailbox state) — pointers + compactions; `fetch_ref` makes dereference one cheap call. But the *historical stream* is owned in-house (Sam's directive 2026-06-12, reversing the earlier pointers-only stance for history): every window's raw lands in `intake`/`archive/` with a `rawness` tag, because external refs rot — servers down, tokens renewed, agents losing context. The record never depends on a live external source.
8. Forgetting third parties: ward members and founders never consented — sensitivity defaults by role context, pastoral content stays out of low-clearance packets structurally.
9. Conflating type and term (P11, Sam's directive 2026-06-12). Claudio is a shippable life-harness (type); Sam's life is its reference term — the test case, never the spec. Any decision resting on an assumption about how the user lives or uses a tool belongs in term config/seeds/regimes, never in schema/functions/normative spec. Both are edited simultaneously every session — name which side each change is on. Terms also mutate over time: interpreting term values are regime-dated, and history is read under historical regimes (his gcal example: planner today, maybe truth in 8 months; both interpretations must coexist). Stdlib corollary (same day, late): between type and term sits the **standard library** — shipped term-shaped defaults (default windows/workflows, starter vocabs, wiki chapters, default parameters) maintained with the type so day one is useful; user overrides are term and survive updates; don't misread stdlib as a type/term leak. Distribution corollary (same day, evening): the type is **installed by default, forkable by choice** — installs are core-owned, read-only to everyone including the user's own sessions; term-building agents structurally cannot touch them; maintainer changes propagate as tagged releases + append-only migrations; the fork test (install+term-build must beat forking on effort) is the packaging acceptance test. Sam has NOT committed to maintaining the type publicly — the distribution design preserves that option cheaply; don't treat it as load-bearing. Audit + packaging path: `docs/type-term-audit.md`, `specs/07 §Distribution`.

## Current state and what happens next

Specs are v4. **P0 is built and P1 is substantially built and verified** (overnight session, 2026-06-12):

- **Repo structure** per `specs/07` stands; `SCHEMA.md` at root is generated (catalog hook), never hand-edited.
- **Eval corpus normalized**: 30 filer fixtures (`evals/filer/`, incl. the gate-critical Thiel/meme/two-episode trio), 10 scenario skeletons, 3 assessment-question method evals. ALL labels are `proposed` — Sam confirms (the "labeled together" half of the P0 gate).
- **L1 is real and green**: `core/l1/migrations/0001-0008` — three planes, FORCE RLS keyed on `session_user` via `l1.clearance()`, kind/jsonb/endpoint validation triggers, ~45 SECURITY DEFINER functions (intent binding with verbatim + stage/read-back/confirm paths, batch `$ref`s, two-lane scoring with floors, poison-pill quarantine, supersedence, merges with guards). **Red-team suite 51/51** (`core/pipes/red-team/run.sh`), **contract tests 76/76** (`evals/contract/run.sh`) on a fresh db. Dev cluster: `./core/deploy/dev.sh start|reset|test` (postgres@17 via brew, `~/.claudio/pg`, socket-only, port 5433, outside iCloud).
- **Deploy artifacts written, not installed**: kill switch (`claudio-stop`), backup + monthly restore-test (destination unconfirmed), `run-worker.sh` (assembles context from agent folders' `context.md` pulls; catalog runs `ok` through it end-to-end), reconciler, launchd template, staged OS-user script. No OS users created, no launchd loaded, no backups configured — those are Sam-present deploy steps.
- **The mirror's folder exists** (`core/agents/mirror/`) and the purpose-contract template (`core/l1/seeds/purpose-contract.md`) awaits the elicitation session.

**What remains before P1 closes**: Sam confirms corpus labels; the elicitation session produces + seeds purpose contract v1 (signed); `setup-os-users.sh p1` + per-uid `.pgpass` + backup destination + restore cron at deploy. The P2-in-two-weeks clock STARTED 2026-06-12 (first DDL) — the daily loop ships by ~2026-06-26 or shrink (honesty gate). Live opens: `docs/questions-queue.md`. Late 2026-06-12: tasks+expectations merged into `obligations` (kind task|expectation; create verbs survive; amend/resolve_obligation lifecycle — Sam's Occam call); `semantics` simplified to scalar-until-first-regime-change; **spec freeze until P2 ships** (new law needs a daily-loop justification). Suites: red-team 51, contract 84. The governing trio for whether this survives: the kill criterion, the effort slider, and the mirror's usage monitoring.

Repo conventions: `specs/` is law; `docs/` is the live workspace (research, evals, queue, audits); `docs/archive/` is the historical brainstorm and review trail — context, never authority; git history is the full audit trail (every design round was committed separately). Today's date matters for "recent": the spec was finalized 2026-06-12.
