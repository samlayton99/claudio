# v3 Diff Log

The one document to review from. Every entry: **your words → what changed → where.** Sources: your line-by-line review (`sam-spec-review.md`), two research passes, four independent audits (UX rings, dependency, malicious red-team, over-engineering). Decline any item individually; specs are at v3.2 (commits `4d7b1c0` → `e733d30`).

## A. Your review, line by line

| Your words | What changed | Where |
|---|---|---|
| "handshake protocol with a dedicated agent... mutually exclusive, collectively exhaustive... security aware" | Kept as a **protocol, not a resident agent** (pushback, accepted in chat): newcomer reads catalog → declares requested scopes → user approves → capability **issued**. Your malicious-hiding worry is solved at issuance: an agent has no write access to hide because it has none until granted. MECE/overlap ceremony deferred to P6 when the first real external agent exists. | `06 §Handshake`, `00 §decay test`, `04 rule 7` |
| "type system should be one of the primary laws... nothing exists without a typecheck" | Constitutional: statuses CHECKed, kinds vocab-validated, **every jsonb payload JSON-Schema-validated by trigger**, refs shape-checked, contract tests on every L1 function; "the type system is the skeleton; the purpose contract is its apex." | `00 §decision map`, `01 §Conventions` |
| "confidence is often poorly judged by LLMs... default low unless glaringly obvious" | Written into P5: confidence defaults LOW; when unsure the system asks taste, not itself. Filer floor + held intake implement it. | `00 P5`, `03 §filer` |
| "approvals/permissions once flagged should be deterministically handled" | Privilege classes derived server-side per action; approval applies in-transaction; arg-predicates; the entire approval surface is deterministic rendering + grants. | `02 §propose/approve_message`, `04 rule 5` |
| "I like the dictation gate, doesn't have to be imessage" | Generalized to "the verified channel" — the edge is a configurable slot; iMessage is just v0's occupant. | `00 §Vocabulary`, `06 §The edge` |
| "run on a vps if desired... design should never prevent a change in environment" | Portability rule: core is POSIX+Postgres+cron; all macOS-isms quarantined in edge+deploy with Linux mappings. | `00 §Portability`, `04 §Availability` |
| "traversal... needs to be the most high quality product... go research" | Research done (`research-traversal.md`): your design's core bets validated (summary atoms + pointers = the measured-best retrieval unit; SQL packets = production pattern). 8 adopted deltas — see §B. | `02`, `01 §Atoms` |
| "relational database very clearly mapped... visually" | Mermaid ER diagram added at the top of the schema. | `01 §entity map` |
| "purpose database... goals, values, attribute_goalposts, priorities... the ultimate typechecking contract... lived vs proclaimed" | **The biggest change in v3.** New purpose plane at the top of the hierarchy: `purpose` table (goal/value/attribute kinds, goalposts, priority_rank) + versioned source markdown (`purpose_versions`), sensitivity 2, user-writable only. Lived-vs-proclaimed = `v_purpose_alignment`, derived never stored. **The mirror** = the one taste-owning agent: elicitation mode (your "FEEL understood" requirement written in) + observational mode. Front-loaded to P0 per the honesty audit. | `01 §Purpose plane`, `03 §The mirror`, `07 §P0/P1` |
| "quality drops exponentially with nested summaries" (your fundamental law) | **Principle P8**, mechanical: compaction depth ≤2; rollups re-derive from atoms only; absolute dates; verbatim `quotes` for load-bearing facts; re-ground from refs before irreversible actions; delta edits only. Research independently evidenced every rule. | `00 P8`, `01 §Atoms`, `05 §Writing discipline` |
| "how are we configuring the agents... swappable by design" | Every judgment step declares harness/model/tools in `config`; swapping is a registry edit. Orchestrator slot explicitly configurable (Claude Code / SDK / Codex / Hermes-like). | `03 §Agent configuration` |
| "distinction between internal writing and external writes... clear boundary with write" | First section of the API spec: L1 = internal only; **external writes are never claudio's** — owning agents fire them post-approval via handoff. Your "central brain, agents reach out" model verbatim. | `02 §intro`, `04 rule 1/4` |
| "names must be super descriptive for mcp" | Naming rule: names ARE the vocabulary (`atoms` not `log`, `record_atom` not `log_entry`); COMMENT ON carries example calls; no bare UUIDs in responses. | `02 §Naming rule`, renames throughout |
| "standard internal format... external automations fit in" | The batch shape + components registry + handshake = the standard wire format; a zapier-ish external automation registers as an outer component speaking L1. Full plug-in framework deliberately not built yet (your "if out of scope, fine"). | `00 §Vocabulary`, `06` |
| "merge decisions should be tunable" | All thresholds in the `parameters` registry (core/outer rings). | `01 §parameters` |
| "retiring a role should not retire wiki pages" | Fixed: `retire_role` never touches pages or atoms; active-roles is a default filter. | `01 §roles`, `02 §retire_role` |
| "proposing messages... email drafting agent owns drafting and sending through its own channels" | Exactly the shipped design: approval acts on claudio surfaces; approved work hands off to the owning agent's own channel. | `02 §approve_message`, `06` |
| "context package... agents not seeing things they don't need... verbose option" | Need-to-know = RLS clearance (structural). Verbose option **cut** by the over-engineering pass: budget is the one dial, drill-down covers depth — flag if you want it back. | `02 §packet` |
| "latency fine on internals, not on user experience" | Edge progress indicators ("on it…"); claim-time lease reaping cut worst-case conversational recovery from 15 min to the next 10s poll. | `06 §Outbound`, `02 §queue_status` |
| "assembler... traverse the graph a little more" | Two-phase protocol: deterministic SQL packet (1-hop) → agentic drill-down for hop 2+ ("start wide, then narrow" — the production-validated pattern). The LLM assembler stays a deferred upgrade wrapping the same signature. | `02 §get_context` |
| "cron times in core configurations" / "hardcoded parameters... unified place, core vs outer" | The `parameters` table, ring-split core/outer, panel-editable outer ring. Seeded minimally (knobs migrate on first tuning). | `01 §parameters` |
| "queues... 100 percent reliable, race conditions" | Conditional transitions, SKIP LOCKED, leases + claim-time reaping, dedup keys, induced-crash P2 gate. | `01 §messages`, `03 §Reliability`, `07 §P2` |
| "security... good and not overkill" | Staged hardening: 2 OS users at P1 → full tiers + pf + Tailscale at P3. Deterministic stops stay; ceremony arrives with its load. | `04 §Staged hardening` |
| "DB needs to be more descriptive" | Descriptive ≠ long: skinny rows + rich COMMENT ON with examples + sample rows in SCHEMA.md (research: schema-in-context reduces hallucination). | `05 §Boundary rule`, `01` header |
| "wiki... research what actually worked" | Done (`research-wiki.md`): plain-files substrate validated emphatically; your 10 chapters = fixed top-level MOCs (the one surviving structure pattern); ≤2-hop reachability. | `05 §Chapters` |
| "wiki pages should not be created for DB events... reads like wikipedia... biography" | Reframed exactly so: the wiki is the biography, the DB is the 1:1 record. Routine meetings = atoms + a line on a page; pages only for significant events. Page creation requires its chapter AND its future read-moment; "interesting in 6 months" default-no. | `05 §intro/§Page discipline` |
| "weekly and monthly summaries... great idea" | Weekly state-of-life digest ships P4; monthly/biannual added **when the weekly is demonstrably read** (over-engineering pass). | `03 §Core workflows` |
| "raw atoms always in context window" when updating pages | Law: update reason + previous version + full relevant atom list; delta edits only; digests never read prior digests. | `05 §Writing discipline` |
| "atoms can be assigned to multiple wiki pages" | Yes — atoms citable by multiple pages. | `05 §Page discipline` |
| "imessage doesn't have to be primary entry" / "dedicated channel... outsourcing feels risky" | The edge: a **required, claudio-owned ground-zero slot**, channel configurable, outsourcing to Poke-class explicitly rejected (it's the untrusted-intake boundary AND the proof-of-user). | `06 §The edge` |
| "orchestrator should be configurable... less hardcoded the better" | Configurable slot + every agent swappable via config. | `03 §Agent configuration` |
| "approval gate... its own channel... configurable" | Approval surfaces: panel + phone (low-risk classes via the edge, server-rendered text). Always claudio-owned (the one non-negotiable). | `02 §edge confirm`, `06` |
| "panel changes override anything... highest authority... permanent artifact" | Panel writes are user-asserted law: `verified_fields`, immutable wiki spans, parameter edits. Chat = equal clearance, panel wins conflicts. | `06 §Panel`, `02 §Dictation gate` |
| "chat interface in panel... place to ask questions, run custom research" | **Cut** by the over-engineering pass: the edge is chat; core sessions are the research surface; two chat surfaces = the duplicate-organ pattern. Returns if phone chat proves too thin. Flag to restore. | `06 §Panel` |
| "custom dashboards can own functionality/metrics" | Dashboards may own derived metrics (`upsert_metric` under their component id). P6. | `06 §Custom dashboards` |
| "evals are not law... build and add as you please" | Written into standing rules; restraint/security cases stay at 100%. | `07 §Standing rules` |
| "folder structure first, types second, wiring third" | The build-order law, verbatim. P0 = structure; P1 = types/contracts; P2+ = wiring. | `07` header, phases |
| "constantly revisit core principles... independent audits... trim fat... ask me questions" | Standing audits at every phase gate (structural, over-engineering, malicious-superintelligence, dependency); cuts go to the queue, never silently dropped. | `07 §Standing rules` |
| Your Q&A answers 1-5 | 1 time-sensitive pushes: in. 2 autonomy: unchanged. 3 (hesitant): context tools stayed, marketplace stayed future. 4 chat=panel clearance, panel supreme: in. 5 router daemon: the edge is named as the one permitted router (conversation only). | `03`, `06` |
| "consolidation/pulse on everything that happened in the day... atom granularity... open question" | Answered: episode atoms unchanged on flood days; the **morning brief writes the daily digest page** (your pulse — folded into the brief by the over-engineering pass: one reflect-organ); packet scoring (weighted sum) keeps volume one pointer away. | `03 §Core workflows`, `01 §Atoms` |
| "vocab seeds... very conservative... require OS to edit... default unknown" | The flag discipline, verbatim: small vocabularies, core-session edits only, documented applications, `unknown` default agents bias toward. | `01 §Kind/flag discipline` |
| "special primary entrypoint... ground zero permissions... required" | The edge, as above — required slot. | `06 §The edge` |
| "budget control... route intelligence... high level" | Model tiers per worker class (cheap default; frontier only for filer + mirror); ONE global monthly ceiling; hygiene spend report. Per-component ceilings cut (single-digit component count). | `03 §Budget control` |
| "cleanest input ever... gardeners minimized... minimax" | **Principle P9** verbatim ("every gardener run is evidence of an upstream design gap"); catalog gardener became a migration hook (the P9 exemplar: deny work by construction). | `00 P9`, `03 §roster` |
| "master password... 2fa pipeline... special verification agent" | Kept as a slot with one amendment (pushback, accepted): verified by **hash, never an agent** (agents can be socially engineered). Deferred to P6 — no operation class currently needs it beyond panel + dictation + intent binding. | `04 rule 3` |
| "write-access agents treated differently... malicious agent would hide this" | Solved deterministically: capability issued never declared; write-capable registrations flagged loudly at the user gate. | `04 rule 7`, `06 §Handshake` |
| "guardrails against unnecessary token spend" | Cost ceilings + `runs.cost_usd` + never-spawn-on-empty-queue + cheap-model defaults + hygiene spend review. | `03` |
| "second agent that has taste... usage monitor... same as purpose-graph agent?" | Same agent — the mirror, two modes (your "easier to own taste in one" — yes). Observational mode = usage monitoring + the alignment surface + taste-write provenance review. | `03 §The mirror` |
| "system prompts suggestions of windows... claude code recon ingester" | Hygiene proposes new windows from usage signals; claude-code-activity window on the P6 list. "Windows" is the first-class term throughout; `general` role exists. | `06 §Windows`, `01 §roles` |
| "clearly mapped place to audit all agents/automations... roles and usage" | The panel registry/audit page over `v_component_health` (role mapping, scopes, usage, cost). | `06 §Panel` |
| "didn't see much planned in terms of inheritance" | Explicit §Inheritance: FK spine → window role_map (your dependent-type framing, verbatim) → person membership → content inference, cheapest first, paths recorded. | `01 §Inheritance` |

## B. Research-driven changes (your directed passes 3 & 6)

Traversal (`research-traversal.md`): scoring product → **weighted sum** with exponential recency decay · timestamps + age rendered inline on every packet item · verbatim-quote discipline (`atoms.quotes`) + re-ground-before-acting · supersedence (`invalidated_at`/`superseded_by`) instead of hard-delete for inferred links · no bare UUIDs, `{id,name}` pairs everywhere · example calls in COMMENT ON + sample rows in SCHEMA.md · wiki edits delta-only · embeddings promotion trigger written down (logged search misses → exact pgvector + RRF; bolt-on, no re-architecture). Validated as-is: summary atoms + refs, tier ladder, SQL packets, lint-not-mirror, episode atomization, no-embeddings-v0.

Wiki (`research-wiki.md`): chapters as fixed MOCs, ≤2-hop rule · page budget vs reading appetite (chapter + read-moment required at creation; demotion sweep P5; sublinear growth) · digests fresh-from-atoms, absolute dates · citation discipline load-bearing (lint checks existence; verifier step checks support) · human corrections = immutable spans + poisoning-propagation walk · anti-slop lint heuristics + lede-first rule · freshness rendered loudly + one-tap correction · no graph features · proposal dedup/expiry.

## C. Audit-driven changes (the four passes you commissioned)

**UX rings** (`docs/ux-rings.md` — the sins list): phone-native approvals for low-risk classes · neglect mode (silence sensor, coalesced alerts, "while you were gone" rollup; 3-week-neglect eval at P5 gate) · Ring-0 collapsed (2 OS users at P1, full tiers P3; gcal via read-only ICS, no OAuth; elicitation as core session) · held-intake TTL → `unknown` atoms, hold-questions ride the brief · drills automated.

**Dependency sweep**: filer marked critical + per-row poison quarantine · `w_approver` extracted from the panel process · handoff `expires_at` (stale approvals never fire late) · proposal regeneration dedup · claim-time lease reaping · query-trigger cycle guard (agent_action excluded by default + chain-depth cap) · edge on-disk spool + `replayable` declarations · purpose-contract absent/stale degraded mode · FileVault/UPS availability honesty · move_page crash order (DB first, lint reconciles).

**Malicious-superintelligence red-team** (the deepest finding of the project): the dictation gate proved to be a *channel* proof, not an *intent* proof — a hijacked agent holding any fresh "ok thanks" could bless attacker text as law. Closed deterministically: **the intent binding** (taste writes require verbatim-in-raw OR edge read-back-confirm via `confirm_taste_write`; no LLM in any taste commit path; the orchestrator stages taste, never writes it) · taste-class non-bundlable + non-standing-approvable, fn→class map core-ring · propose-time own-set validation · server-rendered **what-will-execute** approval view ($refs resolved, confusable-name flags, per-action classes; the agent summary is decoration) · standing approvals gate on arg-predicates, never fn-class alone; merge_people never standing · **injection fence**: `suspected_injection` renders inert everywhere; content tripwire; quarantine-since-T rollback · `how-to-work-with-sam` is taste-plane (rendered from directives; no free gardener deltas) · mirror purpose writes read-back with diff · tool pinning + probation · no caller-controlled URLs for ≥c1 workers · monthly taste-write provenance review.

**Over-engineering pass** (your #1 directive, run last; 35 items — its verdict: the excess was "duplicate organs in the reflect-and-summarize space and fully-built machinery for single-digit traffic"; almost nothing wrong, much of it early). CUT: pulse-as-separate-workflow (brief writes the daily digest) · alignment gardener (mirror observational absorbs it — S4/S5/S7 re-homed) · scout (hygiene one-liner) · standing-approval bootstrap · handshake MECE/sandbox ceremony · packet verbosity knob · Python types entirely, TS types to P3 · panel chat · per-component cost ceilings · proposal impact-ranking · catalog weekly cron (migration hook instead). DEFERRED with laws intact: standing approvals + w_approver → P5 · handshake protocol detail → P6 · metrics table + source-side stats → P5 · state-of-life monthly/biannual → after the weekly proves read · demotion sweep → P5 · notification budget → P5 · master password → P6 slot · response_format detailed/pagination → on observed need · tripwire uid-scans → P3. SIMPLIFIED: verifier = chained step on the wiki gardener (own component only if scale demands) · parameters seeded minimally · panel P3 scope = what the P3 gate exercises · reconciler = core script until P3 · packet weights deliberately unspecced.

## D. Deviations from your words (flagged honestly)

1. **Handshake agent → protocol** (§A row 1) — your sandbox/MECE vision deferred, not deleted.
2. **Master-password verifier = hash, not agent** — an agent in that loop weakens it.
3. **S2's autonomous merge → proposal** — your own P7 (false-positive aversion) outranks your scenario narration; the fixture encodes the proposal path.
4. **S1's silent gcal write → standing approval** — auto only for attendee-free blocks; anything emailing third parties proposes first.
5. **Panel chat cut** — collides with your "chat interface in panel" ask; the over-engineering pass judged it a duplicate organ. Cheapest to restore later if wrong.
6. **"Incessant" alignment → bounded re-ask** — unresolved questions re-surface every report and ride the brief, but P7 caps new questions; true incessance is a directive you can set.

## D2. Post-v3 round: atoms under a real day (your 40-conversation walkthrough)

| Your words | What changed | Where |
|---|---|---|
| "how are atoms made for this window... 40 conversations" | Worked policy: every non-discarded thread-day yields an atom (your meme day = one one-line atom — friends matter value-wise at zero task-yield, your own correction); spam/OTP/bots discard deterministically at the window (`semantics.discard_patterns`), zero filer tokens | `01 §Atoms` |
| "extremely important meeting with Peter Thiel (3 texts) vs family groupchat (75 texts, memes)" | The real gap found and closed: **importance is structural, never volumetric** — `notable` flag (new column, filer-set at P7's "glaringly obvious" bar) + attached obligations + purpose/people/role links + user assertions; message count is never scored | `01 atoms.notable`, `01 §Atoms`, `02 §packet` |
| "some atoms for a 2-hour meeting should be incredibly long, some really short" | Pushback (your P8 is the reason): atom length does NOT scale — destination does. Depth ladder: summary → +detail → +verbatim quotes → +wiki page/section (where "incredibly long" lives) → refs to tier 0 always. The atom is the index card, never the document; a long paraphrase is exactly the nested-summary decay P8 forbids | `01 §Atoms` |
| (implied by your scenario) one thread, two episodes in a day | Loosened: thread-day is the *capture* unit; filer writes 1..n atoms per window, default 1, split only when purposes are glaringly distinct | `01 §Atoms` table |

## E. Still yours to answer (`questions-queue.md` §B)

Purpose-contract sensitivity (default: 2) · first elicitation session timing/format (it now gates P0) · the kill criterion (agree in advance) · monthly budget ceiling (placeholder $50) · wiki default-no bar (confirm) · the five v2-era defaults in §C of the queue.
