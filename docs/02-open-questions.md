# Open Questions

What genuinely remains. (Resolved questions live in the other docs and git history.)

1. **Wiki frontmatter spec** — on hold; review prior art (Obsidian conventions, agent-memory systems) before freezing (`05`).
2. **Dispatcher mechanics** — the LISTEN/NOTIFY pipe that launches agents on queue pings: exactly how it invokes the harness, and which agents are wake-on-ping vs check-on-schedule (`04`).
3. **Assembler scoring** — how the frontier relevance judgment is prompted/calibrated; falls out of building against its eval corpus.

## Deferred capabilities (room reserved, not designed)

- **Computer-use enforcement** (lock content on devices as computer use matures) — the consent pattern (proposals + trust circles) is its slot.
- **High-sensitivity domains** (bank accounts/purchases, medical history) — needs the security tier reserved in `03`.
